From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Lee Jones <joneslee@google.com>
Date: Thu, 25 Jan 2024 15:35:44 +0000
Subject: NOUPSTREAM: ANDROID: build.config.* file hierarchy

This is a squashed commit containing all build.config.* files.

They are not going upstream so history is unimportant in 'common-patches' context.

[CPNOTE: 21/07/21] Lee: Androidness

Signed-off-by: Lee Jones <lee.jones@linaro.org>
Change-Id: I273c3968b70daaf60fd01106fb388475a8645c97
Signed-off-by: Lee Jones <joneslee@google.com>
---
 build.config.aarch64     | 10 ++++++++++
 build.config.arm         | 10 ++++++++++
 build.config.common      | 10 ++++++++++
 build.config.constants   |  1 +
 build.config.db845c      |  3 +++
 build.config.gki         | 11 +++++++++++
 build.config.gki.aarch64 | 20 ++++++++++++++++++++
 build.config.gki.x86_64  | 20 ++++++++++++++++++++
 build.config.x86_64      | 10 ++++++++++
 9 files changed, 95 insertions(+)
 create mode 100644 build.config.aarch64
 create mode 100644 build.config.arm
 create mode 100644 build.config.common
 create mode 120000 build.config.constants
 create mode 100644 build.config.db845c
 create mode 100644 build.config.gki
 create mode 100644 build.config.gki.aarch64
 create mode 100644 build.config.gki.x86_64
 create mode 100644 build.config.x86_64

diff --git a/build.config.aarch64 b/build.config.aarch64
new file mode 100644
--- /dev/null
+++ b/build.config.aarch64
@@ -0,0 +1,10 @@
+echo 'ERROR: Do not source build.config.aarch64.
+    Instead, set kernel_build.make_goals (e.g. ["modules"])
+
+    After you clean up the aforementioned build configs:
+    - If you still have build configs, use:
+        . ${ROOT_DIR}/${KERNEL_DIR}/build.config.constants
+      at the top of your build config, and file a bug with your existing build config.
+    - If you do not have any build configs, delete kernel_build.build_config entirely.
+' >&2
+exit 1
diff --git a/build.config.arm b/build.config.arm
new file mode 100644
--- /dev/null
+++ b/build.config.arm
@@ -0,0 +1,10 @@
+echo 'ERROR: Do not source build.config.arm.
+    Instead, set kernel_build.make_goals (e.g. ["modules"])
+
+    After you clean up the aforementioned build configs:
+    - If you still have build configs, use:
+        . ${ROOT_DIR}/${KERNEL_DIR}/build.config.constants
+      at the top of your build config, and file a bug with your existing build config.
+    - If you do not have any build configs, delete kernel_build.build_config entirely.
+' >&2
+exit 1
diff --git a/build.config.common b/build.config.common
new file mode 100644
--- /dev/null
+++ b/build.config.common
@@ -0,0 +1,10 @@
+echo 'ERROR: Do not source build.config.common. Instead:
+    - If you need -D__ANDROID_COMMON_KERNEL__ in KCFLAGS, set kernel_build.kcflags.
+
+    After you clean up the aforementioned build configs:
+    - If you still have build configs, use:
+        . ${ROOT_DIR}/${KERNEL_DIR}/build.config.constants
+      at the top of your build config, and file a bug with your existing build config.
+    - If you do not have any build configs, delete kernel_build.build_config entirely.
+' >&2
+exit 1
diff --git a/build.config.constants b/build.config.constants
new file mode 120000
--- /dev/null
+++ b/build.config.constants
@@ -0,0 +1 @@
+bazel/constants.scl
\ No newline at end of file
diff --git a/build.config.db845c b/build.config.db845c
new file mode 100644
--- /dev/null
+++ b/build.config.db845c
@@ -0,0 +1,3 @@
+. ${ROOT_DIR}/${KERNEL_DIR}/build.config.constants
+
+DTC_FLAGS="${DTC_FLAGS} -@"
diff --git a/build.config.gki b/build.config.gki
new file mode 100644
--- /dev/null
+++ b/build.config.gki
@@ -0,0 +1,11 @@
+echo 'ERROR: Do not source build.config.gki. Instead:
+    - Set kernel_build.defconfig accordingly (e.g. //common:arch/arm64/configs/gki_defconfig).
+    - Set kernel_build.check_defconfig if necessary (read docs for possible values)
+
+    After you clean up the aforementioned build configs:
+    - If you still have build configs, use:
+        . ${ROOT_DIR}/${KERNEL_DIR}/build.config.constants
+      at the top of your build config, and file a bug with your existing build config.
+    - If you do not have any build configs, delete kernel_build.build_config entirely.
+' >&2
+exit 1
diff --git a/build.config.gki.aarch64 b/build.config.gki.aarch64
new file mode 100644
--- /dev/null
+++ b/build.config.gki.aarch64
@@ -0,0 +1,20 @@
+# Note: This file is no longer used by the GKI to build. The file is kept
+# here because of external usages.
+# TODO(b/236012223): Deprecate and remove this file once build-config-less
+#   builds are fully supported.
+
+echo 'ERROR: Do not source build.config.gki.aarch64.
+
+    - If you need -D__ANDROID_COMMON_KERNEL__ in KCFLAGS, set kernel_build.kcflags.
+      Otherwise leave it empty.
+    - Set kernel_build.make_goals accordingly (e.g. ["modules"])
+    - Set kernel_build.defconfig accordingly (e.g. //common:arch/arm64/configs/gki_defconfig).
+    - Set kernel_build.check_defconfig if necessary (read docs for possible values)
+
+    After you clean up the aforementioned build configs:
+    - If you still have build configs, use:
+        . ${ROOT_DIR}/${KERNEL_DIR}/build.config.constants
+      at the top of your build config, and file a bug with your existing build config.
+    - If you do not have any build configs, delete kernel_build.build_config entirely.
+' >&2
+exit 1
diff --git a/build.config.gki.x86_64 b/build.config.gki.x86_64
new file mode 100644
--- /dev/null
+++ b/build.config.gki.x86_64
@@ -0,0 +1,20 @@
+# Note: This file is no longer used by the GKI to build. The file is kept
+# here because of external usages.
+# TODO(b/236012223): Deprecate and remove this file once build-config-less
+#   builds are fully supported.
+
+echo 'ERROR: Do not source build.config.gki.aarch64.
+
+    - If you need -D__ANDROID_COMMON_KERNEL__ in KCFLAGS, set kernel_build.kcflags.
+      Otherwise leave it empty.
+    - Set kernel_build.make_goals accordingly (e.g. ["modules"])
+    - Set kernel_build.defconfig accordingly (e.g. //common:arch/x86/configs/gki_defconfig).
+    - Set kernel_build.check_defconfig if necessary (read docs for possible values)
+
+    After you clean up the aforementioned build configs:
+    - If you still have build configs, use:
+        . ${ROOT_DIR}/${KERNEL_DIR}/build.config.constants
+      at the top of your build config, and file a bug with your existing build config.
+    - If you do not have any build configs, delete kernel_build.build_config entirely.
+' >&2
+exit 1
diff --git a/build.config.x86_64 b/build.config.x86_64
new file mode 100644
--- /dev/null
+++ b/build.config.x86_64
@@ -0,0 +1,10 @@
+echo 'ERROR: Do not source build.config.x86_64.
+    Instead, set kernel_build.make_goals (e.g. ["modules"])
+
+    After you clean up the aforementioned build configs:
+    - If you still have build configs, use:
+        . ${ROOT_DIR}/${KERNEL_DIR}/build.config.constants
+      at the top of your build config, and file a bug with your existing build config.
+    - If you do not have any build configs, delete kernel_build.build_config entirely.
+' >&2
+exit 1
