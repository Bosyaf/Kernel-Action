From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Lee Jones <joneslee@google.com>
Date: Wed, 22 Jan 2025 10:58:07 +0000
Subject: TODO: usb: gadget: f_fs: Move MAX_ALT_SETTINGS to correct place

Signed-off-by: Lee Jones <joneslee@google.com>
Change-Id: Iac609b8ac9b0b069834e88453cead74fac90a346
---
 drivers/usb/gadget/function/f_fs.c | 6 +++---
 1 file changed, 3 insertions(+), 3 deletions(-)

diff --git a/drivers/usb/gadget/function/f_fs.c b/drivers/usb/gadget/function/f_fs.c
--- a/drivers/usb/gadget/function/f_fs.c
+++ b/drivers/usb/gadget/function/f_fs.c
@@ -3732,13 +3732,13 @@ static int ffs_func_set_alt(struct usb_function *f,
 	struct ffs_data *ffs = func->ffs;
 	int ret = 0, intf;
 
-	if (alt > MAX_ALT_SETTINGS)
-		return -EINVAL;
-
 	intf = ffs_func_revmap_intf(func, interface);
 	if (intf < 0)
 		return intf;
 
+	if (alt > MAX_ALT_SETTINGS)
+		return -EINVAL;
+
 	if (ffs->func)
 		ffs_func_eps_disable(ffs->func);
 
