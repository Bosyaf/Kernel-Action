name: Xiaomi 14 (Houji) Pure Kernel Build

permissions:
  contents: write
  actions: write

on:
  workflow_dispatch:
    inputs:
      variant:
        description: "Build variant (consolidate = tam moduller, gki = sadece GKI)"
        required: true
        default: "consolidate"
        type: choice
        options:
          - consolidate
          - gki
      kernel_name:
        description: "Kernel isim soneki"
        required: false
        default: "Galadriel-Pure"

jobs:
  build-kernel:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    env:
      KBUILD_BUILD_USER: "caglar"
      KBUILD_BUILD_HOST: "guvenc"
      VARIANT: ${{ github.event.inputs.variant }}
      KERNEL_NAME: ${{ github.event.inputs.kernel_name }}

    steps:
      - name: Maximize Build Space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 16384
          temp-reserve-mb: 2048
          swap-size-gb: 16

      - name: Cleanup and Checkout AnyKernel3
        run: |
          rm -rf AnyKernel3
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3 AnyKernel3

      - name: Install Dependencies & Toolchain
        run: |
          sudo apt update -qq
          sudo apt install -y --no-install-recommends \
            python3 git curl libelf-dev build-essential flex bison \
            libssl-dev libncurses-dev liblz4-tool zlib1g-dev \
            libxml2-utils rsync unzip dwarves bc cpio wget \
            xz-utils make gcc-aarch64-linux-gnu pkg-config zstd device-tree-compiler

          mkdir -p $GITHUB_WORKSPACE/toolchain/clang-google
          curl -fLSs "https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/android14-release/clang-r487747c.tar.gz" | tar -xzf - -C $GITHUB_WORKSPACE/toolchain/clang-google
          echo "$GITHUB_WORKSPACE/toolchain/clang-google/bin" >> $GITHUB_PATH

      - name: Clone Kernel Source
        run: |
          git clone -b kplatform https://github.com/mvaisakh/xiaomi14.git source --depth=1

      - name: Fix and Integrate Device Tree Headers
        run: |
          # 1. Harici repoyu çek
          git clone -b kplatform https://github.com/Bosyaf/xiaomi14-devicetrees.git dt_temp --depth=1
          
          # 2. qcom klasörünü yerleştir
          mkdir -p source/arch/arm64/boot/dts/vendor
          cp -r dt_temp/qcom source/arch/arm64/boot/dts/vendor/
          
          # 3. !!! KRİTİK ADIM !!! Eksik Header'ları (dosyaları) kernelin içine aktar
          # Hata aldığımız 'bindings' dosyalarını kernelin ana include yoluna taşıyoruz
          cp -r dt_temp/bindings/* source/include/dt-bindings/
          
          # DTS klasörü içinde de bir kopyasını bulunduralım (Bazı dts'ler göreli yol kullanır)
          mkdir -p source/arch/arm64/boot/dts/vendor/qcom/bindings
          cp -r dt_temp/bindings/* source/arch/arm64/boot/dts/vendor/qcom/bindings/
          
          # 4. Makefile Düzenle (Sadece bizim vendor'u ekle)
          # 'allwinner' hatasından kurtulmak için dts/Makefile'ı temizleyip sadece vendor ekliyoruz
          echo 'subdir-y += vendor' > source/arch/arm64/boot/dts/Makefile
          echo 'subdir-y += qcom' > source/arch/arm64/boot/dts/vendor/Makefile
          
          echo "Headers and Device Tree integrated successfully."

      - name: Prepare Config
        run: |
          cd source
          export PATH="$GITHUB_WORKSPACE/toolchain/clang-google/bin:$PATH"
          OUT_DIR="$GITHUB_WORKSPACE/out"
          mkdir -p "${OUT_DIR}"

          make O="${OUT_DIR}" ARCH=arm64 LLVM=1 gki_defconfig
          scripts/kconfig/merge_config.sh -m -O "${OUT_DIR}" "${OUT_DIR}/.config" arch/arm64/configs/vendor/pineapple_GKI.config || true
          scripts/kconfig/merge_config.sh -m -O "${OUT_DIR}" "${OUT_DIR}/.config" arch/arm64/configs/vendor/houji_GKI.config || true
          
          if [ "$VARIANT" = "consolidate" ]; then
            scripts/kconfig/merge_config.sh -m -O "${OUT_DIR}" "${OUT_DIR}/.config" arch/arm64/configs/consolidate.fragment || true
            scripts/kconfig/merge_config.sh -m -O "${OUT_DIR}" "${OUT_DIR}/.config" arch/arm64/configs/vendor/pineapple_consolidate.config || true
          fi
          
          sed -i "s/CONFIG_LOCALVERSION=.*/CONFIG_LOCALVERSION=\"-${KERNEL_NAME}\"/" "${OUT_DIR}/.config"
          make O="${OUT_DIR}" ARCH=arm64 LLVM=1 olddefconfig

      - name: Build Kernel & DTBs
        run: |
          cd source
          export PATH="$GITHUB_WORKSPACE/toolchain/clang-google/bin:$PATH"
          OUT_DIR="$GITHUB_WORKSPACE/out"

          # DTC (Device Tree Compiler) için include yollarını tanımla
          # -I flagleri her zaman klasörü işaret etmeli
          DTS_BASE="arch/arm64/boot/dts/vendor/qcom"
          DTC_INCLUDE="-I$(pwd)/arch/arm64/boot/dts -I$(pwd)/include -I$(pwd)/$DTS_BASE -I$(pwd)/$DTS_BASE/audio -I$(pwd)/$DTS_BASE/camera -I$(pwd)/$DTS_BASE/display -I$(pwd)/$DTS_BASE/video -I$(pwd)/$DTS_BASE/bindings"

          echo "Building DTBs..."
          make -j$(nproc --all) O="${OUT_DIR}" ARCH=arm64 LLVM=1 LLVM_IAS=1 \
               DTC_FLAGS="${DTC_INCLUDE} -Wno-graph_child_address -Wno-interrupt_provider" \
               dtbs

          echo "Building Kernel Image..."
          make -j$(nproc --all) O="${OUT_DIR}" ARCH=arm64 LLVM=1 LLVM_IAS=1 Image

      - name: Build & Install Modules
        if: github.event.inputs.variant == 'consolidate'
        run: |
          cd source
          export PATH="$GITHUB_WORKSPACE/toolchain/clang-google/bin:$PATH"
          OUT_DIR="$GITHUB_WORKSPACE/out"
          MOD_DIR="$GITHUB_WORKSPACE/modules_temp"
          mkdir -p "${MOD_DIR}"

          make -j$(nproc --all) O="${OUT_DIR}" ARCH=arm64 LLVM=1 LLVM_IAS=1 modules
          make O="${OUT_DIR}" ARCH=arm64 LLVM=1 INSTALL_MOD_PATH="${MOD_DIR}" modules_install
          find "${MOD_DIR}" -name "*.ko" -exec llvm-strip --strip-debug {} \;

      - name: Package with AnyKernel3
        run: |
          OUT_DIR="$GITHUB_WORKSPACE/out"
          MOD_DIR="$GITHUB_WORKSPACE/modules_temp"
          AK3_DIR="$GITHUB_WORKSPACE/AnyKernel3"

          # Dosyaları topla
          cp "${OUT_DIR}/arch/arm64/boot/Image" "$AK3_DIR/Image"
          mkdir -p "$AK3_DIR/dtb" "$AK3_DIR/dtbo"
          find "${OUT_DIR}/arch/arm64/boot/dts/vendor/qcom" -name "*.dtb" -exec cp {} "$AK3_DIR/dtb/" \; 2>/dev/null || true
          find "${OUT_DIR}/arch/arm64/boot/dts/vendor/qcom" -name "*.dtbo" -exec cp {} "$AK3_DIR/dtbo/" \; 2>/dev/null || true

          if [ -d "${MOD_DIR}/lib/modules" ]; then
            mkdir -p "$AK3_DIR/modules/vendor/lib/modules"
            find "${MOD_DIR}/lib/modules" -name "*.ko" -exec cp {} "$AK3_DIR/modules/vendor/lib/modules/" \;
          fi

          # AnyKernel3 Ayarları
          cd "$AK3_DIR"
          sed -i "s/kernel.string=.*/kernel.string=CG+ $KERNEL_NAME ($VARIANT)/" anykernel.sh
          sed -i 's/do.devicecheck=1/do.devicecheck=0/' anykernel.sh
          sed -i '/is_slot_device=auto/a device.name1=houji\ndevice.name2=shennong\ndevice.name3=xiaomi14' anykernel.sh

          ZIP_NAME="Houji-PureKernel-${VARIANT}-$(date +%Y%m%d-%H%M).zip"
          zip -r "../${ZIP_NAME}" ./*
          echo "ZIP_NAME=${ZIP_NAME}" >> $GITHUB_ENV
          echo "BOOTHASH=Pure-Houji-$(date +%Y%m%d-%H%M)" >> $GITHUB_ENV

      - name: Upload Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.BOOTHASH }}
          name: "Xiaomi 14 (Houji) ${{ env.VARIANT }} - $(date +%Y-%m-%d)"
          body: |
            ## Galadriel Pure Kernel
            - **Header Fix:** Audio and Camera headers integrated into include/dt-bindings.
            - **DTC Fix:** Cleaned include paths for vendor tree.
            - **Modules:** All included.
          files: "${{ env.ZIP_NAME }}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
