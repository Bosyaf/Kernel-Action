name: Xiaomi 14 (Houji) Pure Final Fix

permissions:
  contents: write
  actions: write

on:
  workflow_dispatch:
    inputs:
      kernel_name:
        description: "Kernel İsmi"
        required: false
        default: "Galadriel-Pure"

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      KBUILD_BUILD_USER: "caglar"
      KBUILD_BUILD_HOST: "guvenc"
    steps:
      - name: Maximize Space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 16384
          temp-reserve-mb: 2048
          swap-size-gb: 16

      - name: 1. Toolchain ve dwarves (BTF için)
        run: |
          sudo apt update -qq && sudo apt install -y python3 git curl build-essential flex bison libssl-dev libelf-dev bc cpio zip dwarves
          mkdir -p $GITHUB_WORKSPACE/clang-zyc
          curl -fLSs "https://github.com/ZyCromerZ/Clang/releases/download/22.0.0git-20250928-release/Clang-22.0.0git-20250928.tar.gz" | tar -xzf - -C $GITHUB_WORKSPACE/clang-zyc
          echo "$GITHUB_WORKSPACE/clang-zyc/bin" >> $GITHUB_PATH

      - name: 2. Kaynak Kodu Çek ve OTOMATİK TEMİZLE
        run: |
          # Source'u çekiyoruz
          git clone -b android14-6.1-staging https://github.com/Bosyaf/android_kernel_common-6.1.git source
          cd source
          
          # !!! BURASI ÇOK KRİTİK: Manuel yüklediğimiz hatalı header'ları temizliyoruz !!!
          # include klasörünü orijinal haline döndürür
          git checkout include/
          # Fazladan eklenen dosyaları (bindings vb.) uçurur
          git clean -fd include/
          
          echo "✅ Klasör yapısı orijinal haline döndürüldü, kirlilik temizlendi."

      - name: 3. Config Merge
        run: |
          cd source
          export PATH="$GITHUB_WORKSPACE/clang-zyc/bin:$PATH"
          OUT_DIR=$GITHUB_WORKSPACE/out
          mkdir -p $OUT_DIR
          
          # Senin attığın configleri birleştiriyoruz
          scripts/kconfig/merge_config.sh -m -O $OUT_DIR \
            arch/arm64/configs/gki_defconfig \
            arch/arm64/configs/vendor/pineapple_GKI.config \
            arch/arm64/configs/vendor/houji_GKI.config \
            arch/arm64/configs/consolidate.fragment \
            arch/arm64/configs/vendor/pineapple_consolidate.config \
            arch/arm64/configs/vendor/houji_consolidate.config

          sed -i "s/CONFIG_LOCALVERSION=.*/CONFIG_LOCALVERSION=\"-${{ github.event.inputs.kernel_name }}\"/" $OUT_DIR/.config
          
          # BTF'yi açık tutuyoruz (Açılması için şart)
          scripts/config --file $OUT_DIR/.config --enable CONFIG_DEBUG_INFO_BTF
          
          make O=$OUT_DIR ARCH=arm64 LLVM=1 LLVM_IAS=1 olddefconfig

      - name: 4. Sadece Image ve Modülleri Derle
        run: |
          cd source
          export PATH="$GITHUB_WORKSPACE/clang-zyc/bin:$PATH"
          OUT_DIR=$GITHUB_WORKSPACE/out
          
          # DTB derlemiyoruz (Hata riskini sıfıra indirdik)
          make -j$(nproc --all) O=$OUT_DIR ARCH=arm64 LLVM=1 LLVM_IAS=1 Image modules

      - name: 5. Paketleme (AnyKernel3 + Hazır DTBO'lar)
        run: |
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3 AnyKernel3
          AK3=$GITHUB_WORKSPACE/AnyKernel3
          OUT=$GITHUB_WORKSPACE/out
          SRC=$GITHUB_WORKSPACE/source
          
          # 1. Kendi Image'ımızı al
          cp $OUT/arch/arm64/boot/Image $AK3/
          
          # 2. Hazır Donanım Dosyalarını (Dersimizi aldık, hatasız olanları alıyoruz)
          mkdir -p $AK3/dtb $AK3/dtbo
          find $SRC/houji_prebuilts -name "*.dtb" -exec cp {} $AK3/dtb/ \; 2>/dev/null || true
          find $SRC/houji_prebuilts -name "*.dtbo" -exec cp {} $AK3/dtbo/ \; 2>/dev/null || true
          
          # 3. Kendi modüllerimizi al
          mkdir -p $AK3/modules/vendor/lib/modules
          find $OUT -name "*.ko" -exec cp {} $AK3/modules/vendor/lib/modules/ \;

          # 4. AnyKernel ayarları
          cd $AK3
          sed -i "s/kernel.string=.*/kernel.string=CG+ ${{ github.event.inputs.kernel_name }}/" anykernel.sh
          sed -i '/is_slot_device=auto/a device.name1=houji\ndevice.name2=shennong\ndevice.name3=xiaomi14' anykernel.sh

          zip -r "../Houji-Kernel-Pure.zip" ./*

      - name: 6. Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: Houji-${{ github.run_id }}
          name: "Xiaomi 14 Pure Kernel"
          files: "Houji-Kernel-Pure.zip"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
