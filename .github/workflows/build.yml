name: Xiaomi 14 Houji - Hata Yakalama

permissions:
  contents: write

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Maximize Build Space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 8192
          temp-reserve-mb: 2048
          swap-size-gb: 8

      - name: Install Dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y \
            bc bison build-essential flex git libssl-dev \
            libelf-dev python3 wget zip cpio curl dwarves which pahole
          pahole --version

      - name: Setup Neutron Clang
        run: |
          mkdir -p "$HOME/toolchains/neutron-clang"
          cd "$HOME/toolchains/neutron-clang"
          curl -LO "https://raw.githubusercontent.com/Neutron-Toolchains/antman/main/antman"
          chmod +x antman
          ./antman -S
          echo "$HOME/toolchains/neutron-clang/bin" >> $GITHUB_PATH

      - name: Clone Kernel Source
        run: |
          git clone -b monolithic-test-2 https://github.com/mvaisakh/xiaomi14.git --depth=1 kernel

      - name: Build Kernel (Hatayı Yakalamak İçin)
        continue-on-error: true
        run: |
          cd kernel
          export PATH="$HOME/toolchains/neutron-clang/bin:$PATH"
          
          make O=out ARCH=arm64 LLVM=1 LLVM_IAS=1 houji_defconfig
          
          echo "🚀 Derleme başlıyor, wlan hatası bekleniyor..."
          make O=out ARCH=arm64 LLVM=1 LLVM_IAS=1 -j$(nproc --all) 2>&1 | tee ../build.log

      - name: Hatayı Ekrana Bas
        run: |
          echo "======================================="
          echo "🔥 WLAN / DERLEME HATASI BURADA 🔥"
          echo "======================================="
          grep -n -i -E "error:|fatal error:|undefined reference|No rule to make target" build.log | tail -n 50 || true
          echo "======================================="
          echo "SON 50 SATIR:"
          tail -n 50 build.log || true

      - name: Upload Log Dosyası
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: build.log
