name: CG+ Yuki Gaming Kernel

permissions:
  contents: write
  actions: write

on:
  workflow_dispatch:

jobs:
  build-kernel:
    runs-on: ubuntu-latest
    timeout-minutes: 420
    env:
      KBUILD_BUILD_USER: "caglar"
      KBUILD_BUILD_HOST: "guvenc"

    steps:
      - name: Maximize Space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 8192
          temp-reserve-mb: 2048
          swap-size-gb: 8

      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache Clang
        id: cache-clang
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/clang-yuki
          key: clang-yuki-bolt-20260124
          restore-keys: |
            clang-yuki-

      - name: Download Clang
        if: steps.cache-clang.outputs.cache-hit != 'true'
        run: |
          mkdir -p $GITHUB_WORKSPACE/clang-yuki
          curl -L "https://usc1.contabostorage.com/ab57618a161b456a8d009c2e2de4a408:xperience/toolchains/yuki-clang-+bolt-20260124.tar.zst" | tar --zstd -xC $GITHUB_WORKSPACE/clang-yuki

      - name: 1. Paketler
        run: |
          sudo apt update -qq
          sudo apt install -y --no-install-recommends \
            python3 git curl libelf-dev build-essential flex bison \
            libssl-dev libncurses-dev liblz4-tool zlib1g-dev libxml2-utils \
            rsync unzip dwarves bc cpio wget xz-utils make gcc-aarch64-linux-gnu \
            lld llvm pkg-config zstd patch

      - name: 2. Kaynakları Çek
        run: |
          git clone https://github.com/osm0sis/AnyKernel3.git --depth 1 AnyKernel3
          git clone -b android14-6.1-staging https://github.com/ramabondanp/android_kernel_common-6.1.git --depth=1 common

      # ========================
      # 🔥 KERNELSU-NEXT (FİZİKSEL KOPYALAMA - GARANTİ YÖNTEM)
      # ========================
      - name: 3. KernelSU-Next Setup
        run: |
          cd common
          
          # 1. Temizlik
          rm -rf drivers/kernelsu KernelSU-Next
          sed -i '/kernelsu/d' drivers/Kconfig || true
          sed -i '/kernelsu/d' drivers/Makefile || true

          # 2. KernelSU-Next indir
          git clone https://github.com/pershoot/KernelSU-Next.git -b dev-susfs --depth=1 KernelSU-Next
          
          # 3. Multi-Manager Patch Uygula (Herhangi bir APK için)
          cd KernelSU-Next
          patch -p1 < $GITHUB_WORKSPACE/patches/ksu/ksun-add-more-managers-support.patch || echo "⚠️ Multi-mgr patch atlandı"
          cd ..

          # 4. Dosyaları drivers/kernelsu içine FİZİKSEL olarak kopyala (Symlink hatasını önler)
          mkdir -p drivers/kernelsu
          cp -rf KernelSU-Next/kernel/* drivers/kernelsu/
          
          # 5. Kernel'e tanıt
          echo 'source "drivers/kernelsu/Kconfig"' >> drivers/Kconfig
          echo 'obj-y += kernelsu/' >> drivers/Makefile
          
          # 6. UAPI Fix
          cp -rf KernelSU-Next/userspace/include/uapi/* include/uapi/ 2>/dev/null || true

          # 7. KESİN KONTROL
          echo "🔍 Dosya kontrolü:"
          if [ -f "drivers/kernelsu/Kbuild" ]; then
            echo "✅ KernelSU başarıyla yerleştirildi!"
          else
            echo "❌ HATA: Dosyalar kopyalanamadı!"
            exit 1
          fi

      # ========================
      # 🔥 SUSFS + 5 HOTFIX (SİMONPUNK)
      # ========================
      - name: 4. SUSFS Setup
        run: |
          cd common
          SUSFS_BRANCH="gki-android14-6.1"
          
          # simonpunk/susfs4ksu indir
          git clone --depth=1 https://gitlab.com/simonpunk/susfs4ksu.git -b $SUSFS_BRANCH susfs
          
          # Dosyaları yerleştir
          cp -r susfs/kernel_patches/fs/* fs/
          cp -r susfs/kernel_patches/include/* include/
          
          # Ana patch
          patch -p1 < susfs/kernel_patches/50_add_susfs_in_${SUSFS_BRANCH}.patch || true
          
          # 🛠️ 5 HOTFIX 🛠️
          
          # FIX 1: namespace.c (Jump label & definitions)
          cat > /tmp/ns_inject.txt << 'EOF'
          #ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
          #include <linux/susfs_def.h>
          extern bool susfs_is_current_ksu_domain(void);
          extern struct static_key_false susfs_set_sdcard_android_data_decrypted_key_false;
          #define CL_COPY_MNT_NS BIT(25)
          static DEFINE_IDA(susfs_mnt_id_ida);
          static DEFINE_IDA(susfs_mnt_group_ida);
          #define DEFAULT_KSU_MNT_ID 500000
          #define DEFAULT_KSU_MNT_GROUP_ID 5000
          #define VFSMOUNT_MNT_FLAGS_KSU_UNSHARED_MNT 0x80000000
          #endif
          EOF
          sed -i '/#include "internal.h"/r /tmp/ns_inject.txt' fs/namespace.c
          
          # FIX 2: statfs.c (CRC fix)
          sed -i '/#include <linux\/susfs_def.h>/i #ifndef __GENKSYMS__' fs/statfs.c || true
          sed -i '/#include <linux\/susfs_def.h>/a #endif' fs/statfs.c || true
          
          # FIX 3: fs_proc_base.c (Senin yüklediğin patch)
          patch -p1 < $GITHUB_WORKSPACE/patches/susfs/fs_proc_base.c-fix-k6.1.patch || true
          
          # FIX 4: extras.c (AVC weak symbols)
          if [ -f "drivers/kernelsu/extras.c" ]; then
            cat > /tmp/avc_fix.txt << 'EOF'
          #ifdef CONFIG_KSU_SUSFS
          #ifndef susfs_avc_log_spoofing_key_true
          __attribute__((weak))
          struct static_key_true susfs_avc_log_spoofing_key_true = STATIC_KEY_TRUE_INIT;
          #endif
          #endif
          EOF
            sed -i '/#include/r /tmp/avc_fix.txt' drivers/kernelsu/extras.c
          fi
          
          # FIX 5: supercalls.c (Uname spoof fix)
          if [ -f "drivers/kernelsu/supercalls.c" ]; then
            sed -i 's/#ifdef CONFIG_KSU_SUSFS_SPOOF_UNAME/#if 0 \/\* Disabled *\//' drivers/kernelsu/supercalls.c || true
          fi
          
          echo "✅ SUSFS ve 5 Hotfix tamamlandı!"

      - name: 5. Wi-Fi ve Asya Yamaları
        run: |
          cd common
          curl -fLSs https://github.com/OnePlus-12-Development/android_kernel_qcom_sm8650/commit/3e0cb08.patch > wifi.patch || true
          [ -s wifi.patch ] && patch -p1 < wifi.patch || true
          wget -O- https://github.com/vc-teahouse/Baseband-guard/raw/main/setup.sh | bash || true

      - name: 5.5. Özel Performans
        run: |
          cd common
          [ -d "../patches" ] && for f in ../patches/*.patch; do patch -p1 --forward < "$f" || true; done

      - name: 6. Full Kernel Config
        run: |
          cd common
          export PATH="$GITHUB_WORKSPACE/clang-yuki/bin:$PATH"
          make O=out ARCH=arm64 LLVM=1 LLVM_IAS=1 gki_defconfig
          
          CONFIG="out/.config"
          # KSU + SUSFS Configs
          ./scripts/config --file $CONFIG --enable CONFIG_KSU
          ./scripts/config --file $CONFIG --enable CONFIG_KSU_SUSFS
          ./scripts/config --file $CONFIG --enable CONFIG_KSU_SUSFS_SUS_PATH
          ./scripts/config --file $CONFIG --enable CONFIG_KSU_SUSFS_SUS_MOUNT
          ./scripts/config --file $CONFIG --enable CONFIG_KSU_SUSFS_SUS_KSTAT
          ./scripts/config --file $CONFIG --enable CONFIG_KSU_SUSFS_SUS_KSTAT_SPOOF_GENERIC
          ./scripts/config --file $CONFIG --enable CONFIG_KSU_SUSFS_TRY_UMOUNT
          ./scripts/config --file $CONFIG --enable CONFIG_KSU_SUSFS_SPOOF_UNAME
          ./scripts/config --file $CONFIG --enable CONFIG_KSU_SUSFS_OPEN_REDIRECT
          ./scripts/config --file $CONFIG --enable CONFIG_KSU_SUSFS_SUS_SU
          ./scripts/config --file $CONFIG --enable CONFIG_KSU_MULTI_MANAGER_SUPPORT
          ./scripts/config --file $CONFIG --enable CONFIG_KSU_ALLOWLIST_WORKAROUND
          
          ./scripts/config --file $CONFIG --set-str CONFIG_LOCALVERSION "-C-G+"
          ./scripts/config --file $CONFIG --disable CONFIG_LOCALVERSION_AUTO
          
          make O=out ARCH=arm64 LLVM=1 LLVM_IAS=1 olddefconfig
          grep "CONFIG_KSU" $CONFIG

      - name: 7. Kernel Derleme
        run: |
          cd common
          export PATH="$GITHUB_WORKSPACE/clang-yuki/bin:$PATH"
          make -j$(nproc --all) O=out ARCH=arm64 LLVM=1 LLVM_IAS=1 \
            CROSS_COMPILE=aarch64-linux-gnu- \
            KCFLAGS="-O2 -march=armv9.2-a+crypto+sha3+crc+dotprod+rcpc+lse -falign-functions=64" \
            Image || exit 1
          echo "BOOTHASH=Yuki-$(date +%s)" >> $GITHUB_ENV

      - name: 8. AnyKernel3
        run: |
          cd ./AnyKernel3
          sed -i 's/kernel.string=.*/kernel.string=CG+ Yuki Balanced Pro/' anykernel.sh
          sed -i 's/do.devicecheck=1/do.devicecheck=0/' anykernel.sh
          # Cihaz isimlerini güvenli şekilde ekle
          echo "device.name1=houji" >> anykernel.sh
          echo "device.name2=shennong" >> anykernel.sh
          echo "device.name3=aurora" >> anykernel.sh
          echo "device.name4=xiaomi14" >> anykernel.sh
          cp ../common/out/arch/arm64/boot/Image .
          zip -r "../CG-Yuki-Balanced-Pro.zip" ./*

      - name: 9. Release Yükle
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.BOOTHASH }}
          name: "CG+ Yuki Clang (Balanced Pro)"
          files: "*.zip"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
