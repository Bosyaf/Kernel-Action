name: Xiaomi 14 Monolithic Build (houji)

permissions:
  contents: write
  actions: write

on:
  workflow_dispatch:

jobs:
  build-kernel:
    runs-on: ubuntu-latest
    timeout-minutes: 420
    env:
      KBUILD_BUILD_USER: "caglar"
      KBUILD_BUILD_HOST: "guvenc"

    steps:
      - name: Maximize Space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 8192
          temp-reserve-mb: 2048
          swap-size-gb: 16 # Monolithic derleme daha fazla RAM/Swap isteyebilir

      - name: Checkout
        uses: actions/checkout@v4

      - name: 1. Paketler ve ZyC Clang Çekimi
        run: |
          sudo apt update -qq
          sudo apt install -y --no-install-recommends \
            python3 git curl libelf-dev build-essential flex bison \
            libssl-dev libncurses-dev liblz4-tool zlib1g-dev libxml2-utils \
            rsync unzip dwarves bc cpio wget xz-utils make gcc-aarch64-linux-gnu \
            pkg-config zstd
            
          echo "🔥 Toolchain: ZyC Clang 22"
          CLANG_URL="https://github.com/ZyCromerZ/Clang/releases/download/22.0.0git-20250928-release/Clang-22.0.0git-20250928.tar.gz"
          
          mkdir -p $GITHUB_WORKSPACE/clang-zyc
          curl -fLSs "$CLANG_URL" | tar -xzf - -C $GITHUB_WORKSPACE/clang-zyc
          echo "$GITHUB_WORKSPACE/clang-zyc/bin" >> $GITHUB_PATH
          
      - name: 2. Kaynakları Çek (MVaisakh Monolithic)
        run: |
          git clone https://github.com/osm0sis/AnyKernel3.git --depth 1 AnyKernel3
          # MVaisakh'ın özel monolithic-test-2 dalını çekiyoruz
          git clone -b monolithic-test-2 https://github.com/mvaisakh/xiaomi14.git --depth=1 common

      - name: 3. Kernel Config Hazırlığı (houji_defconfig)
        run: |
          cd common
          export PATH="$GITHUB_WORKSPACE/clang-zyc/bin:$PATH"
          
          # Monolithic yapıda .scmversion sorun çıkarabilir
          touch .scmversion
          
          # Klasik GKI yerine MVaisakh'ın houji_defconfig dosyasını kullanıyoruz
          make O=out ARCH=arm64 LLVM=1 LLVM_IAS=1 \
            CC="clang" LD="ld.lld" AR="llvm-ar" NM="llvm-nm" \
            OBJCOPY="llvm-objcopy" OBJDUMP="llvm-objdump" STRIP="llvm-strip" \
            houji_defconfig

      - name: 4. Kernel Derleme
        run: |
          cd common
          export PATH="$GITHUB_WORKSPACE/clang-zyc/bin:$PATH"
          
          # MVaisakh'ın config'indeki her şeyi (WALT dahil) çekirdeğe gömerek derler
          make -j$(nproc --all) O=out ARCH=arm64 LLVM=1 LLVM_IAS=1 \
            CROSS_COMPILE=aarch64-linux-gnu- \
            CC="clang" LD="ld.lld" AR="llvm-ar" NM="llvm-nm" \
            OBJCOPY="llvm-objcopy" OBJDUMP="llvm-objdump" STRIP="llvm-strip" \
            KCFLAGS="-O2 \
            -march=armv9.2-a+crypto+crc+dotprod+rcpc+lse \
            -falign-loops=32 \
            -fomit-frame-pointer \
            -Wno-error" \
            KAFLAGS="-march=armv9.2-a+crypto+crc+dotprod+rcpc+lse" \
            Image || { echo "❌ Derleme HATASI!"; exit 1; }
          
          echo "BOOTHASH=Houji-Mono-$(date +%s)" >> $GITHUB_ENV
          
      - name: 5. AnyKernel3 Paketleme
        run: |
          cd ./AnyKernel3
          sed -i 's/kernel.string=.*/kernel.string=Xiaomi 14 Monolithic Test/' anykernel.sh
          sed -i 's/do.devicecheck=1/do.devicecheck=0/' anykernel.sh
          
          # Monolithic yapıda genelde modül (.ko) oluşmaz, her şey Image içindedir.
          cp ../common/out/arch/arm64/boot/Image .
          
          # Eğer yine de modül oluşursa onları da alalım (tedbir amaçlı)
          mkdir -p modules/vendor/lib/modules
          find ../common/out -name "*.ko" -exec cp {} modules/vendor/lib/modules/ \; || true
          
          zip -r "../Xiaomi14-Monolithic.zip" ./*

      - name: 6. Release Yükle
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.BOOTHASH }}
          name: "Xiaomi 14 Monolithic (houji_defconfig)"
          files: "*.zip"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
