name: CG+ Yuki Gaming Kernel (KSU-Next + SUSFS + BBG)

permissions:
  contents: write
  actions: write

on:
  workflow_dispatch:

jobs:
  build-kernel:
    runs-on: ubuntu-latest
    timeout-minutes: 420
    env:
      KBUILD_BUILD_USER: "caglar"
      KBUILD_BUILD_HOST: "guvenc"

    steps:
      - name: Maximize Space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 8192
          temp-reserve-mb: 2048
          swap-size-mb: 8192

      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache Clang
        id: cache-clang
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/clang-zyc
          key: clang-zyc-22-20250928
          restore-keys: |
            clang-zyc-

      - name: Download Clang
        if: steps.cache-clang.outputs.cache-hit != 'true'
        run: |
          echo "🔥 ZyC Clang 22 indiriliyor..."
          CLANG_URL="https://github.com/ZyCromerZ/Clang/releases/download/22.0.0git-20250928-release/Clang-22.0.0git-20250928.tar.gz"
          curl -fLSs "$CLANG_URL" -o clang.tar.gz
          mkdir -p $GITHUB_WORKSPACE/clang-zyc
          tar -xzf clang.tar.gz -C $GITHUB_WORKSPACE/clang-zyc
          rm -f clang.tar.gz

      - name: 1. Paketler
        run: |
          sudo apt update -qq
          sudo apt install -y --no-install-recommends \
            python3 git curl libelf-dev build-essential flex bison \
            libssl-dev libncurses-dev liblz4-tool zlib1g-dev libxml2-utils \
            rsync unzip dwarves bc cpio wget xz-utils make gcc-aarch64-linux-gnu \
            lld llvm pkg-config zstd jq

      - name: 2. Kaynakları Çek
        run: |
          git clone https://github.com/osm0sis/AnyKernel3.git --depth 1 AnyKernel3
          git clone -b android14-6.1-staging https://github.com/ramabondanp/android_kernel_common-6.1.git --depth=1 common

      - name: 3. Pershoot KSU-Next & MultiManager (Yol Düzeltildi)
        run: |
          cd common
          echo "🔐 Pershoot KernelSU-Next (dev-susfs) çekiliyor..."
          git clone -b dev-susfs https://github.com/pershoot/KernelSU-Next.git drivers/kernelsu
          
          echo "🛠️ MultiManager yaması uygulanıyor..."
          # Yol kontrolü: manager.c artık 'kernel/' klasörü içinde
          MGR_FILE="drivers/kernelsu/kernel/manager.c"
          if [ ! -f "$MGR_FILE" ]; then MGR_FILE="drivers/kernelsu/manager.c"; fi
          
          if [ -f "$MGR_FILE" ]; then
            echo "✅ Dosya bulundu: $MGR_FILE"
            sed -i 's/static bool is_manager_apk(const char \*package_name)/static bool is_manager_apk(const char \*package_name) { return true; }\nstatic bool is_manager_apk_unused(const char \*package_name)/' "$MGR_FILE"
            sed -i 's/if (!check_manager_signature(task))/if (false \&\& !check_manager_signature(task))/' "$MGR_FILE"
          else
            echo "❌ HATA: manager.c bulunamadı!"
            exit 1
          fi

          # Kconfig/Makefile (Kök dizinde kalmaya devam ediyorlar)
          if ! grep -q "kernelsu/Kconfig" drivers/Kconfig; then
            sed -i '/^endmenu/i source "drivers/kernelsu/Kconfig"' drivers/Kconfig
          fi
          if ! grep -q "kernelsu" drivers/Makefile; then
            echo 'obj-$(CONFIG_KSU) += kernelsu/' >> drivers/Makefile
          fi

      - name: 4. Pershoot SUSFS Entegrasyonu
        run: |
          cd common
          echo "🛡️ Pershoot SUSFS4KSU (6.1-dev) entegre ediliyor..."
          git clone -b gki-android14-6.1-dev https://gitlab.com/pershoot/susfs4ksu.git susfs_repo
          
          # 1. SUSFS Core Dosyaları
          mkdir -p fs/susfs
          cp -rf susfs_repo/fs/susfs/* fs/susfs/
          cp -rf susfs_repo/include/linux/susfs.h include/linux/
          cp -rf susfs_repo/include/linux/susfs_def.h include/linux/ 2>/dev/null || true
          
          # 2. VFS Yamalarını Uygula (Pershoot'un yöntemi - KSU-Next hooks zaten dev-susfs'te var)
          echo "📦 Çekirdek VFS yamaları kopyalanıyor..."
          cp -f susfs_repo/kernel_patches/fs/* fs/
          cp -f susfs_repo/kernel_patches/include/linux/* include/linux/
          
          # 3. Kconfig/Makefile entegrasyonu
          if ! grep -q "susfs/Kconfig" fs/Kconfig; then
            sed -i '/^endmenu/i source "fs/susfs/Kconfig"' fs/Kconfig
          fi
          if ! grep -q "susfs" fs/Makefile; then
            echo 'obj-$(CONFIG_KSU_SUSFS) += susfs/' >> fs/Makefile
          fi
          rm -rf susfs_repo

      - name: 5. BBG Entegrasyonu
        run: |
          cd common
          echo "📡 BBG entegre ediliyor..."
          git clone https://github.com/vc-teahouse/Baseband-guard.git --depth=1 bbg_repo
          mkdir -p drivers/bbg
          cp -rf bbg_repo/drivers/bbg/* drivers/bbg/ 2>/dev/null || cp -rf bbg_repo/* drivers/bbg/
          echo -e 'obj-$(CONFIG_BBG) += bbg.o' > drivers/bbg/Makefile
          echo -e 'config BBG\n\tbool "Baseband Guard"\n\tdefault n' > drivers/bbg/Kconfig
          sed -i '/^endmenu/i source "drivers/bbg/Kconfig"' drivers/Kconfig
          echo 'obj-$(CONFIG_BBG) += bbg/' >> drivers/Makefile
          rm -rf bbg_repo

      - name: 6. Config & Patch
        run: |
          cd common
          export PATH="$GITHUB_WORKSPACE/clang-zyc/bin:$PATH"
          make O=out ARCH=arm64 LLVM=1 LLVM_IAS=1 gki_defconfig
          
          CONFIG_FILE="out/.config"
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_KSU
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_KSU_SUSFS
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_KSU_SUSFS_SUS_PATH
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_KSU_SUSFS_SUS_MOUNT
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_KSU_SUSFS_SUS_KSTAT
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_BBG
          ./scripts/config --file $CONFIG_FILE --set-val CONFIG_HZ 300
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_PREEMPT
          ./scripts/config --file $CONFIG_FILE --set-str CONFIG_DEFAULT_TCP_CONG "bbr"
          ./scripts/config --file $CONFIG_FILE --set-str CONFIG_LOCALVERSION "-CG+-Yuki-Multi-SUSFS"
          
          make O=out ARCH=arm64 LLVM=1 LLVM_IAS=1 olddefconfig

      - name: 7. Derleme
        run: |
          cd common
          export PATH="$GITHUB_WORKSPACE/clang-zyc/bin:$PATH"
          make -j$(nproc --all) O=out ARCH=arm64 LLVM=1 LLVM_IAS=1 \
            CROSS_COMPILE=aarch64-linux-gnu- \
            KCFLAGS="-O2 -march=armv9.2-a+crypto -fomit-frame-pointer" \
            Image
          
          echo "BOOTHASH=Yuki-$(date +%Y%m%d-%H%M)" >> $GITHUB_ENV

      - name: 8. Paketleme
        run: |
          cd AnyKernel3
          sed -i 's/kernel.string=.*/kernel.string=CG+ Yuki MultiManager + SUSFS/' anykernel.sh
          cp ../common/out/arch/arm64/boot/Image .
          ZIP_NAME="CG-Yuki-MultiKSU-SUSFS-$(date +%Y%m%d-%H%M).zip"
          zip -r9 "../${ZIP_NAME}" ./*
          echo "ZIP_NAME=$ZIP_NAME" >> $GITHUB_ENV

      - name: 9. Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.BOOTHASH }}
          name: "🎮 Yuki Gaming Kernel (MultiManager + Pershoot SUSFS)"
          body: |
            - KernelSU-Next (Pershoot dev-susfs)
            - SUSFS v1.5.1 (Pershoot Method)
            - MultiManager Support (Any Package Name)
            - HZ=300 Gaming Performance
            - Baseband Guard (BBG)
          files: "*.zip"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
