name: CG+ ZyC Gaming Kernel

permissions:
  contents: write
  actions: write

on:
  workflow_dispatch:

jobs:
  build-kernel:
    runs-on: ubuntu-latest
    timeout-minutes: 420
    env:
      KBUILD_BUILD_USER: "caglar"
      KBUILD_BUILD_HOST: "guvenc"

    steps:
      - name: Maximize Space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 8192
          temp-reserve-mb: 2048
          swap-size-gb: 8

      - name: Checkout
        uses: actions/checkout@v4

      - name: 1. Paketler ve ZYC CLANG Çekimi
        run: |
          sudo apt update -qq
          sudo apt install -y --no-install-recommends \
            python3 git curl libelf-dev build-essential flex bison \
            libssl-dev libncurses-dev liblz4-tool zlib1g-dev libxml2-utils \
            rsync unzip dwarves bc cpio wget xz-utils make gcc-aarch64-linux-gnu \
            pkg-config zstd
          
          echo "🔥 ZyC Clang 22 (Eylül 2025 - ARMv9 Uyumlu)"
          CLANG_URL="https://github.com/ZyCromerZ/Clang/releases/download/22.0.0git-20250928-release/Clang-22.0.0git-20250928.tar.gz"
          
          curl -fLSs "$CLANG_URL" -o clang.tar.gz || { echo "❌ HATA: Clang indirilemedi!"; exit 1; }
          
          mkdir -p $GITHUB_WORKSPACE/clang-zyc
          tar -xzf clang.tar.gz -C $GITHUB_WORKSPACE/clang-zyc
          rm -f clang.tar.gz
          
          CLANG_BIN_PATH=$(find $GITHUB_WORKSPACE/clang-zyc -type d -name "bin" | head -n 1)
          echo "✅ Clang Bin: $CLANG_BIN_PATH"
          echo "CLANG_BIN_PATH=$CLANG_BIN_PATH" >> $GITHUB_ENV
          
          $CLANG_BIN_PATH/clang --version || { echo "❌ Clang çalışmıyor!"; exit 1; }

      - name: 2. Kaynakları Çek
        run: |
          git clone https://github.com/osm0sis/AnyKernel3.git --depth 1 AnyKernel3
          git clone -b android14-6.1-staging https://github.com/ramabondanp/android_kernel_common-6.1.git --depth=1 common

      - name: 3. VortexSU Kurulumu
        run: |
          cd common
          curl -LSs "https://raw.githubusercontent.com/Kingfinik98/VortexSU/refs/heads/main/kernel/setup.sh" | bash -s main

      - name: 4. Wi-Fi ve BBRv3 Yamaları
        run: |
          cd common
          
          # Wi-Fi Patch
          curl -fLSs https://github.com/OnePlus-12-Development/android_kernel_qcom_sm8650/commit/3e0cb08.patch > wifi.patch || true
          if [ -s wifi.patch ] && grep -q "diff" wifi.patch; then
            patch -p1 --forward < wifi.patch || echo "⚠️ Wi-Fi patch atlandı"
          fi
          
          # BBRv3 Patch (başarısız olursa devam et)
          patch -p1 --fuzz=3 < $GITHUB_WORKSPACE/patches/bbrv3.patch || echo "⚠️ BBRv3 patch başarısız, standart BBR kullanılacak"
          
          # Baseband Guard (Asya Anti-Cheat)
          wget -O- https://github.com/vc-teahouse/Baseband-guard/raw/main/setup.sh | bash || echo "⚠️ BBG kurulumu atlandı"
          sed -i '/^config LSM$/,/^help$/{ /^[[:space:]]*default/ { /baseband_guard/! s/lockdown/lockdown,baseband_guard/ } }' security/Kconfig || true

      - name: 5. SUSFS Entegrasyonu
        run: |
          cd common
          
          # SUSFS Klonla
          git clone --depth=1 -q https://gitlab.com/simonpunk/susfs4ksu -b gki-android14-6.1 ../susfs
          cp -R ../susfs/kernel_patches/fs/* ./fs/
          cp -R ../susfs/kernel_patches/include/* ./include/
          
          # SUSFS Ana Patch
          if ! patch -p1 --forward < ../susfs/kernel_patches/50_add_susfs_in_gki-android14-6.1.patch; then
            echo "❌ SUSFS patch BAŞARISIZ - Devam edilemiyor!"
            exit 1
          fi
          
          # SUSFS Fix Patch
          git clone https://github.com/Kingfinik98/build-vortex.git ../build-vortex
          patch -p1 --forward < ../build-vortex/kernel-patches/susfs/fs_proc_base.c-fix-k6.1.patch || echo "⚠️ SUSFS fix patch atlandı"
          
          # Namespace.c İnjection
          cat << 'EOF' > .ns_inject_tmp
          #ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
          #include <linux/susfs_def.h>
          extern bool susfs_is_current_ksu_domain(void);
          extern bool susfs_is_current_zygote_domain(void);
          extern bool susfs_is_boot_completed_triggered;
          extern bool susfs_is_sdcard_android_data_decrypted;
          static DEFINE_IDA(susfs_mnt_id_ida);
          static DEFINE_IDA(susfs_mnt_group_ida);
          #define CL_COPY_MNT_NS BIT(25)
          #endif
          EOF
          sed -i '/#include "internal.h"/r .ns_inject_tmp' ./fs/namespace.c
          rm -f .ns_inject_tmp
          
          # Statfs.c Fix
          sed -i '/#include <linux\/susfs_def.h>/i #ifndef __GENKSYMS__' fs/statfs.c || true
          sed -i '/#include <linux\/susfs_def.h>/a #endif' fs/statfs.c || true

      - name: 6. Kernel Konfigürasyonu
        run: |
          cd common
          CONFIG_FILE="arch/arm64/configs/gki_defconfig"
          
          # ═══════════════════════════════════════════════════════════
          # 🎯 CMDLINE (Temiz ve Minimal - Debug Yükü Temizlendi)
          # ═══════════════════════════════════════════════════════════
          ./scripts/config --file $CONFIG_FILE \
            --set-str CONFIG_CMDLINE "console=ttynull stack_depot_disable=on bootconfig quiet slub_debug=-"
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_CMDLINE_EXTEND
          
          # ═══════════════════════════════════════════════════════════
          # ⚡ HZ AYARLARI (250Hz - Denge: Performans + Batarya)
          # ═══════════════════════════════════════════════════════════
          ./scripts/config --file $CONFIG_FILE --disable CONFIG_HZ_100
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_HZ_250
          ./scripts/config --file $CONFIG_FILE --disable CONFIG_HZ_300
          ./scripts/config --file $CONFIG_FILE --disable CONFIG_HZ_1000
          ./scripts/config --file $CONFIG_FILE --set-val CONFIG_HZ 250
          
          # ═══════════════════════════════════════════════════════════
          # 🚀 PREEMPT (Full Preemption - Düşük Latency)
          # ═══════════════════════════════════════════════════════════
          ./scripts/config --file $CONFIG_FILE --disable CONFIG_PREEMPT_NONE
          ./scripts/config --file $CONFIG_FILE --disable CONFIG_PREEMPT_VOLUNTARY
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_PREEMPT
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_PREEMPT_BUILD
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_PREEMPT_COUNT
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_PREEMPTION
          ./scripts/config --file $CONFIG_FILE --disable CONFIG_PREEMPT_DYNAMIC
          
          # ═══════════════════════════════════════════════════════════
          # 🔋 TICK (NO_HZ_IDLE - Batarya Tasarrufu)
          # ═══════════════════════════════════════════════════════════
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_NO_HZ_IDLE
          ./scripts/config --file $CONFIG_FILE --disable CONFIG_NO_HZ_FULL
          
          # ═══════════════════════════════════════════════════════════
          # 🎮 CPU FREQ (Schedutil - Geniş Uyumluluk)
          # ═══════════════════════════════════════════════════════════
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_CPU_FREQ
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_CPU_FREQ_GOV_SCHEDUTIL
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_CPU_FREQ_DEFAULT_GOV_SCHEDUTIL
          ./scripts/config --file $CONFIG_FILE --set-str CONFIG_CPU_FREQ_DEFAULT_GOV "schedutil"
          
          # WALT varsa ekle (yoksa hata vermez)
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_CPU_FREQ_GOV_WALT || echo "⚠️ WALT yok, Schedutil kullanılacak"
          
          # ═══════════════════════════════════════════════════════════
          # 💤 CPU IDLE (TEO - Hızlı Wake-up)
          # ═══════════════════════════════════════════════════════════
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_CPU_IDLE
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_ARM_PSCI_CPUIDLE
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_ARM_CPUIDLE
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_CPU_IDLE_GOV_TEO
          ./scripts/config --file $CONFIG_FILE --disable CONFIG_CPU_IDLE_GOV_MENU
          
          # ═══════════════════════════════════════════════════════════
          # 🌡️ THERMAL (Step Wise - Stabil Throttling)
          # ═══════════════════════════════════════════════════════════
          ./scripts/config --file $CONFIG_FILE --disable CONFIG_THERMAL_GOV_FAIR_SHARE
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_THERMAL_GOV_STEP_WISE
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_THERMAL_GOV_POWER_ALLOCATOR
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_THERMAL_GOV_USER_SPACE
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_THERMAL_DEFAULT_GOV_STEP_WISE
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_SCHED_THERMAL_PRESSURE
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_CPU_THERMAL
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_DEVFREQ_THERMAL
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_QCOM_TSENS
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_QCOM_SPMI_TEMP_ALARM
          
          # ═══════════════════════════════════════════════════════════
          # 🧠 SCHEDULER (EAS + UCLAMP - Akıllı Güç Yönetimi)
          # ═══════════════════════════════════════════════════════════
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_SCHED_MC
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_UCLAMP_TASK
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_UCLAMP_TASK_GROUP
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_ENERGY_MODEL
          
          # ═══════════════════════════════════════════════════════════
          # 🔓 KPROBES (KernelSU Zorunluluğu)
          # ═══════════════════════════════════════════════════════════
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_KPROBES
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_HAVE_KPROBES
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_KPROBE_EVENTS
          
          # ═══════════════════════════════════════════════════════════
          # 🔥 LTO (Thin LTO - Hız/Boyut Dengesi)
          # ═══════════════════════════════════════════════════════════
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_LTO_CLANG
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_LTO_CLANG_THIN
          ./scripts/config --file $CONFIG_FILE --disable CONFIG_LTO_CLANG_FULL
          
          # ═══════════════════════════════════════════════════════════
          # 🗑️ DEBUG KAPATMA (Binary Boyut -30MB)
          # ═══════════════════════════════════════════════════════════
          ./scripts/config --file $CONFIG_FILE --disable CONFIG_NUMA_BALANCING
          ./scripts/config --file $CONFIG_FILE --disable CONFIG_DEBUG_INFO_REDUCED
          ./scripts/config --file $CONFIG_FILE --disable CONFIG_DEBUG_INFO_COMPRESSED
          ./scripts/config --file $CONFIG_FILE --disable CONFIG_DEBUG_INFO_SPLIT
          ./scripts/config --file $CONFIG_FILE --disable CONFIG_DEBUG_INFO_DWARF4
          ./scripts/config --file $CONFIG_FILE --disable CONFIG_DEBUG_INFO_DWARF5
          ./scripts/config --file $CONFIG_FILE --disable CONFIG_DEBUG_INFO_BTF
          
          # ═══════════════════════════════════════════════════════════
          # 🌐 AĞ (BBR + FQ_CODEL + WireGuard)
          # ═══════════════════════════════════════════════════════════
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_TCP_CONG_ADVANCED
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_TCP_CONG_BBR
          ./scripts/config --file $CONFIG_FILE --set-str CONFIG_DEFAULT_TCP_CONG "bbr"
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_NET_SCH_FQ
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_NET_SCH_FQ_CODEL
          ./scripts/config --file $CONFIG_FILE --set-str CONFIG_DEFAULT_NET_SCH "fq_codel"
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_WIREGUARD
          
          # ═══════════════════════════════════════════════════════════
          # 🛡️ SUSFS + BBG + OVERLAY
          # ═══════════════════════════════════════════════════════════
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_BBG
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_OVERLAY_FS
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_TMPFS_XATTR
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_TMPFS_POSIX_ACL
          
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_KSU_SUSFS
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_KSU_SUSFS_SUS_PATH
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_KSU_SUSFS_SUS_MOUNT
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_KSU_SUSFS_SUS_KSTAT
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_KSU_SUSFS_SUS_OVERLAYFS
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_KSU_SUSFS_TRY_UMOUNT
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_KSU_SUSFS_SPOOF_UNAME
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_KSU_SUSFS_ENABLE_LOG
          
          # ═══════════════════════════════════════════════════════════
          # 📛 LOCALVERSION
          # ═══════════════════════════════════════════════════════════
          ./scripts/config --file $CONFIG_FILE --set-str CONFIG_LOCALVERSION "-Laurelin-X"
          ./scripts/config --file $CONFIG_FILE --disable CONFIG_LOCALVERSION_AUTO
          
          sed -i 's/check_defconfig//' build.config.gki || true

      - name: 7. Kernel Derleme
        run: |
          cd common
          
          export PATH="${{ env.CLANG_BIN_PATH }}:$PATH"
          export LD_LIBRARY_PATH="${{ env.CLANG_BIN_PATH }}/../lib:$LD_LIBRARY_PATH"
          
          echo "🔍 Clang Versiyonu:"
          clang --version
          
          touch .scmversion
          
          # defconfig uygula
          make O=out ARCH=arm64 LLVM=1 LLVM_IAS=1 gki_defconfig
          make O=out ARCH=arm64 LLVM=1 LLVM_IAS=1 olddefconfig
          
          echo "🔍 Config Kontrolü:"
          grep -E "CONFIG_CPU_FREQ_DEFAULT_GOV=|CONFIG_HZ=|CONFIG_PREEMPT=|CONFIG_THERMAL_DEFAULT_GOV" out/.config || true
          
          # Derleme Başlat
          make -j$(nproc --all) O=out ARCH=arm64 LLVM=1 LLVM_IAS=1 \
            CROSS_COMPILE=aarch64-linux-gnu- \
            KCFLAGS="-O2 \
                -march=armv8.2-a+crypto+dotprod+fp16 \
                -mtune=cortex-a76.cortex-a55 \
                -fno-asynchronous-unwind-tables \
                -fno-unwind-tables \
                -falign-functions=32 \
                -fomit-frame-pointer" \
            Image || { echo "❌ Derleme BAŞARISIZ!"; exit 1; }
          
          echo "BOOTHASH=ZyC-$(date +%s%N | cut -c1-10)" >> $GITHUB_ENV

      - name: 8. AnyKernel3 Paketleme
        run: |
          cd ./AnyKernel3
          
          sed -i 's/kernel.string=.*/kernel.string=CG+ ZyC Balanced Pro Kernel/' anykernel.sh
          sed -i 's/do.devicecheck=1/do.devicecheck=0/' anykernel.sh
          sed -i 's/is_slot_device=0/is_slot_device=auto/' anykernel.sh
          sed -i '/is_slot_device=auto/a device.name1=houji\ndevice.name2=shennong\ndevice.name3=aurora\ndevice.name4=xiaomi14' anykernel.sh
          
          cp ../common/out/arch/arm64/boot/Image .
          zip -r "../CG-ZyC-Balanced-Pro.zip" ./*

      - name: 9. Release Yayınla
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.BOOTHASH }}
          name: "CG+ ZyC Clang 22 (Balanced Pro)"
          body: |
            ## 📦 Kernel Bilgileri
            - **Clang:** ZyC 22.0.0 (Eylül 2025)
            - **Arch:** ARMv8.2-a + Crypto + FP16
            - **HZ:** 250 (Dengeli)
            - **Preempt:** Full (Düşük Latency)
            - **Governor:** Schedutil (WALT fallback)
            - **TCP:** BBR (v3 patch uygulandı)
            - **Thermal:** Step Wise
            - **Özellikler:** VortexSU + SUSFS + BBG + WireGuard
          files: "*.zip"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
