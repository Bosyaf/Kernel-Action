name: CG+ ZyC Gaming Kernel (Full LTO)

permissions:
  contents: write
  actions: write

on:
  workflow_dispatch:

jobs:
  build-kernel:
    runs-on: ubuntu-latest
    timeout-minutes: 420
    env:
      KBUILD_BUILD_USER: "caglar"
      KBUILD_BUILD_HOST: "guvenc"

    steps:
      - name: Maximize Space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 6144
          temp-reserve-mb: 1024
          swap-size-gb: 20

      - name: Checkout
        uses: actions/checkout@v4

      - name: 1. Paketler ve ZYC CLANG Çekimi (Kusursuz Denge)
        run: |
          sudo apt update -qq
          sudo apt install -y --no-install-recommends \
            python3 git curl libelf-dev build-essential flex bison \
            libssl-dev libncurses-dev liblz4-tool zlib1g-dev libxml2-utils \
            rsync unzip dwarves bc cpio wget xz-utils make gcc-aarch64-linux-gnu \
            lld llvm pkg-config zstd
          
          echo "🔥 Kusursuz Denge: ZyC Clang 22 (Eylül 2025 Sürümü - ARMv9-a Uyumlu)"
          CLANG_URL="https://github.com/ZyCromerZ/Clang/releases/download/22.0.0git-20250928-release/Clang-22.0.0git-20250928.tar.gz"
          echo "ZyC Clang 22 indiriliyor: $CLANG_URL"
          
          curl -fLSs "$CLANG_URL" -o clang.tar.gz || { echo "HATA: Clang URL'si patlak (404)!"; exit 1; }
          
          echo "Arşiv Çıkarılıyor..."
          mkdir -p $GITHUB_WORKSPACE/clang-zyc
          tar -xzf clang.tar.gz -C $GITHUB_WORKSPACE/clang-zyc
          rm -f clang.tar.gz

      - name: 1.5. Full LTO Bellek Optimizasyonu (zram + Swap Ayarları)
        run: |
          echo "🧠 Full LTO için agresif bellek yönetimi kuruluyor..."
          
          sudo modprobe zram num_devices=1 2>/dev/null || true
          if [ -e /sys/block/zram0/disksize ]; then
            echo "📦 zram0 kuruluyor (8GB, lz4 sıkıştırma)..."
            sudo swapoff /dev/zram0 2>/dev/null || true
            echo 1 | sudo tee /sys/block/zram0/reset > /dev/null 2>&1 || true
            echo lz4 | sudo tee /sys/block/zram0/comp_algorithm 2>/dev/null || \
            echo lzo | sudo tee /sys/block/zram0/comp_algorithm 2>/dev/null || true
            echo 8G | sudo tee /sys/block/zram0/disksize
            sudo mkswap /dev/zram0
            sudo swapon -p 100 /dev/zram0
            echo "✅ zram0 aktif (yüksek öncelik)"
          fi
          
          sudo sysctl -w vm.overcommit_memory=1
          sudo sysctl -w vm.swappiness=180
          sudo sysctl -w vm.vfs_cache_pressure=500
          sudo sysctl -w vm.dirty_ratio=5
          sudo sysctl -w vm.dirty_background_ratio=2
          sudo sysctl -w vm.max_map_count=2097152
          sudo sysctl -w vm.page-cluster=0
          
          echo ""
          echo "📊 Bellek Durumu:"
          free -h
          echo ""
          echo "📊 Swap Alanları:"
          swapon --show
          echo ""
          echo "📊 Toplam Kullanılabilir (RAM+Swap):"
          awk '/MemTotal/{ram=$2} /SwapTotal/{swap=$2} END{printf "RAM: %.1f GB | Swap: %.1f GB | Toplam: %.1f GB\n", ram/1048576, swap/1048576, (ram+swap)/1048576}' /proc/meminfo

      - name: 2. Kaynakları Çek (Yeni Staging Dalı)
        run: |
          git clone https://github.com/osm0sis/AnyKernel3.git --depth 1 AnyKernel3
          git clone -b android14-6.1-staging https://github.com/ramabondanp/android_kernel_common-6.1.git --depth=1 common

      - name: 3. Full Kernel Config (Full LTO + 🎭 Müşaviri Kandırma)
        run: |
          cd common
          
          CONFIG_FILE="arch/arm64/configs/gki_defconfig"
          
          ./scripts/config --file $CONFIG_FILE --set-str CONFIG_CMDLINE "console=ttynull stack_depot_disable=on cgroup_disable=pressure kasan.page_alloc.sample=10 kasan.stacktrace=off kvm-arm.mode=protected bootconfig ioremap_guard"
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_CMDLINE_EXTEND
          
          ./scripts/config --file $CONFIG_FILE --disable CONFIG_LTO_NONE
          ./scripts/config --file $CONFIG_FILE --disable CONFIG_LTO_CLANG_THIN
          ./scripts/config --file $CONFIG_FILE --disable CONFIG_THINLTO
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_LTO_CLANG
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_LTO_CLANG_FULL
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_CC_OPTIMIZE_FOR_PERFORMANCE
          
          # 🎭 TABELA ASILIYOR: Xiaomi modülleri görsün diye güvenlik şalterleri "AÇIK" gösteriliyor
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_CFI
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_CFI_CLANG
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_KCFI
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_ARCH_USES_CFI_TRAPS
          ./scripts/config --file $CONFIG_FILE --enable CONFIG_SHADOW_CALL_STACK
          
          ./scripts/config --file $CONFIG_FILE --disable CONFIG_NUMA_BALANCING
          ./scripts/config --file $CONFIG_FILE --disable CONFIG_DEBUG_INFO_REDUCED
          ./scripts/config --file $CONFIG_FILE --disable CONFIG_DEBUG_INFO_COMPRESSED
          ./scripts/config --file $CONFIG_FILE --disable CONFIG_DEBUG_INFO_SPLIT
          ./scripts/config --file $CONFIG_FILE --disable CONFIG_DEBUG_INFO_DWARF4
          ./scripts/config --file $CONFIG_FILE --disable CONFIG_DEBUG_INFO_DWARF5
          
          ./scripts/config --file $CONFIG_FILE --set-str CONFIG_LOCALVERSION "-C-G+ZyC+FullLTO"
          ./scripts/config --file $CONFIG_FILE --disable CONFIG_LOCALVERSION_AUTO
          sed -i 's/check_defconfig//' build.config.gki || true

      - name: 4. Kernel Derleme (Full LTO)
        run: |
          cd common
          export PATH="$GITHUB_WORKSPACE/clang-zyc/bin:$PATH"
          
          sed -i 's/^PHONY += all/  /g' Makefile || true
          touch .scmversion
          
          cat << 'EOF' > scripts/check-local-export
          #!/bin/sh
          exit 0
          EOF
          chmod +x scripts/check-local-export
          
          make O=out ARCH=arm64 LLVM=1 LLVM_IAS=1 \
            CC="clang" LD="ld.lld" HOSTCC="clang" HOSTCXX="clang++" HOSTLD="ld.lld" \
            AR="llvm-ar" NM="llvm-nm" OBJCOPY="llvm-objcopy" OBJDUMP="llvm-objdump" STRIP="llvm-strip" \
            gki_defconfig
          
          make O=out ARCH=arm64 LLVM=1 LLVM_IAS=1 \
            CC="clang" LD="ld.lld" HOSTCC="clang" HOSTCXX="clang++" HOSTLD="ld.lld" \
            AR="llvm-ar" NM="llvm-nm" OBJCOPY="llvm-objcopy" OBJDUMP="llvm-objdump" STRIP="llvm-strip" \
            olddefconfig
          
          echo ""
          echo "⚡ FULL LTO DOĞRULAMASI:"
          grep -E "CONFIG_LTO_CLANG_FULL|CONFIG_LTO_CLANG_THIN|CONFIG_LTO_CLANG=|CONFIG_LTO_NONE" out/.config || true
          echo ""
          echo "🛡️ CFI/KCFI/SCS TABELA DOĞRULAMASI (=y OLMALI):"
          grep -E "CONFIG_CFI_CLANG|CONFIG_CFI=|CONFIG_KCFI|CONFIG_ARCH_USES_CFI_TRAPS|CONFIG_SHADOW_CALL_STACK" out/.config || true
          echo ""
          
          # Full LTO kontrol
          if grep -q "CONFIG_LTO_CLANG_FULL=y" out/.config; then
            echo "✅ Full LTO AKTIF"
          else
            echo "⚠️ Full LTO aktif DEĞİL, zorla açılıyor..."
            sed -i '/CONFIG_LTO_NONE/d' out/.config
            sed -i '/CONFIG_LTO_CLANG_THIN/d' out/.config
            sed -i '/CONFIG_THINLTO/d' out/.config
            echo "CONFIG_LTO_CLANG=y" >> out/.config
            echo "CONFIG_LTO_CLANG_FULL=y" >> out/.config
            echo "# CONFIG_LTO_CLANG_THIN is not set" >> out/.config
            echo "# CONFIG_LTO_NONE is not set" >> out/.config
            
            make O=out ARCH=arm64 LLVM=1 LLVM_IAS=1 \
              CC="clang" LD="ld.lld" HOSTCC="clang" HOSTCXX="clang++" HOSTLD="ld.lld" \
              AR="llvm-ar" NM="llvm-nm" OBJCOPY="llvm-objcopy" OBJDUMP="llvm-objdump" STRIP="llvm-strip" \
              olddefconfig
          fi
          
          echo ""
          echo "🔍 Final config:"
          grep -E "CONFIG_LTO_CLANG_FULL|CONFIG_KCFI|CONFIG_CFI_CLANG|CONFIG_CFI=|CONFIG_SHADOW_CALL_STACK|CONFIG_HZ=|CONFIG_PREEMPT=" out/.config || true
          echo ""
          echo "📊 Derleme öncesi bellek:"
          free -h
          
          sync && echo 3 | sudo tee /proc/sys/vm/drop_caches > /dev/null
          
          echo ""
          echo "🔨 Full LTO derleme başlıyor (Kandırmaca Modu Aktif)..."
          echo ""
          
          # ⚡ ALTIN VURUŞ BURADA: KCFLAGS içine -fno-sanitize=kcfi eklendi!
          make -j$(nproc --all) O=out ARCH=arm64 LLVM=1 LLVM_IAS=1 \
            CROSS_COMPILE=aarch64-linux-gnu- \
            CC="clang" LD="ld.lld" HOSTCC="clang" HOSTCXX="clang++" HOSTLD="ld.lld" \
            AR="llvm-ar" NM="llvm-nm" OBJCOPY="llvm-objcopy" OBJDUMP="llvm-objdump" STRIP="llvm-strip" \
            KCFLAGS="-O2 -march=armv9.2-a+crypto+crc -fno-stack-protector -fomit-frame-pointer -fno-unwind-tables -fno-sanitize=kcfi -Wno-constant-logical-operand" \
            Image || { echo "❌ Derleme HATASI!"; free -h; exit 1; }
          
          echo ""
          echo "✅ Full LTO derleme BAŞARILI!"
          echo "BOOTHASH=ZyC-FullLTO-$(date +%s%N | cut -c1-10)" >> $GITHUB_ENV

      - name: 5. AnyKernel3
        run: |
          cd ./AnyKernel3
          sed -i 's/kernel.string=.*/kernel.string=CG+ ZyC Full LTO Balanced Pro Kernel/' anykernel.sh
          sed -i 's/do.devicecheck=1/do.devicecheck=0/' anykernel.sh
          sed -i 's/is_slot_device=0/is_slot_device=auto/' anykernel.sh
          sed -i '/is_slot_device=auto/a device.name1=houji\ndevice.name2=shennong\ndevice.name3=aurora\ndevice.name4=xiaomi14' anykernel.sh
          
          cp ../common/out/arch/arm64/boot/Image .
          zip -r "../CG-ZyC-FullLTO-Balanced-Pro.zip" ./*

      - name: 6. Release Yükle
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.BOOTHASH }}
          name: "CG+ ZyC Clang (Full LTO - Balanced Pro - Altın Denge)"
          files: "*.zip"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          
