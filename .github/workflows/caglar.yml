name: MI14 GKI Vendor Visibility Test

permissions:
  contents: write
  actions: write

on:
  workflow_dispatch:

jobs:
  build-kernel:
    runs-on: ubuntu-latest
    timeout-minutes: 420

    env:
      KBUILD_BUILD_USER: "caglar"
      KBUILD_BUILD_HOST: "guvenc"
      GOOGLE_CLANG_REV: "r487747c"

    steps:
      - name: Maximize Space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 4096
          temp-reserve-mb: 1024
          swap-size-gb: 32

      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache Google Clang 17
        id: cache-clang
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/google-clang
          key: google-clang-${{ env.GOOGLE_CLANG_REV }}

      - name: Install packages
        run: |
          sudo apt update -qq
          sudo apt install -y --no-install-recommends \
            python3 git curl libelf-dev build-essential flex bison \
            libssl-dev libncurses-dev liblz4-tool zlib1g-dev libxml2-utils \
            rsync unzip dwarves bc cpio wget xz-utils make gcc-aarch64-linux-gnu \
            lld llvm pkg-config zstd zip patch pahole ca-certificates

      - name: Download Google Clang 17
        if: steps.cache-clang.outputs.cache-hit != 'true'
        run: |
          mkdir -p "$GITHUB_WORKSPACE/google-clang"
          cd "$GITHUB_WORKSPACE/google-clang"
          CLANG_URL="https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/main/clang-${GOOGLE_CLANG_REV}.tar.gz"
          echo "Downloading ${CLANG_URL}"
          curl -L "${CLANG_URL}" | tar -xz
          test -x "$GITHUB_WORKSPACE/google-clang/bin/clang"

      - name: Show Clang version
        run: |
          "$GITHUB_WORKSPACE/google-clang/bin/clang" --version
          "$GITHUB_WORKSPACE/google-clang/bin/ld.lld" --version || true

      - name: Hardened Memory
        run: |
          sudo modprobe zram num_devices=1 2>/dev/null || true
          if [ -e /sys/block/zram0/disksize ]; then
            sudo swapoff /dev/zram0 2>/dev/null || true
            echo 1 | sudo tee /sys/block/zram0/reset >/dev/null 2>&1 || true
            echo lz4 | sudo tee /sys/block/zram0/comp_algorithm >/dev/null 2>&1 || true
            echo 16G | sudo tee /sys/block/zram0/disksize >/dev/null
            sudo mkswap /dev/zram0
            sudo swapon -p 100 /dev/zram0
          fi

          sudo sysctl -w vm.overcommit_memory=1
          sudo sysctl -w vm.swappiness=180
          sudo sysctl -w vm.vfs_cache_pressure=500
          sudo sysctl -w vm.dirty_ratio=10
          sudo sysctl -w vm.dirty_background_ratio=5
          sudo sysctl -w vm.max_map_count=2097152
          sudo sysctl -w vm.page-cluster=0

          free -h
          swapon --show

      - name: Clone sources
        run: |
          git clone https://github.com/osm0sis/AnyKernel3.git --depth 1 AnyKernel3
          git clone -b android14-6.1-staging \
            https://github.com/ramabondanp/android_kernel_common-6.1.git \
            --depth=1 common

      - name: Configure kernel
        run: |
          cd common
          export PATH="$GITHUB_WORKSPACE/google-clang/bin:$PATH"

          CONFIG_FILE="arch/arm64/configs/gki_defconfig"

          # Senin çalışan kernel cmdline'ına yakın kısa blok
          ./scripts/config --file "$CONFIG_FILE" \
            --set-str CONFIG_CMDLINE "console=ttynull stack_depot_disable=on cgroup_disable=pressure kasan.page_alloc.sample=10 kasan.stacktrace=off kvm-arm.mode=protected bootconfig ioremap_guard"
          ./scripts/config --file "$CONFIG_FILE" --enable CONFIG_CMDLINE_EXTEND

          # Deepongi'ye yaklaş
          ./scripts/config --file "$CONFIG_FILE" --enable CONFIG_LTO_CLANG
          ./scripts/config --file "$CONFIG_FILE" --enable CONFIG_LTO_CLANG_FULL
          ./scripts/config --file "$CONFIG_FILE" --disable CONFIG_LTO_NONE
          ./scripts/config --file "$CONFIG_FILE" --disable CONFIG_LTO_CLANG_THIN || true
          ./scripts/config --file "$CONFIG_FILE" --disable CONFIG_THINLTO || true
          ./scripts/config --file "$CONFIG_FILE" --enable CONFIG_CC_OPTIMIZE_FOR_PERFORMANCE

          ./scripts/config --file "$CONFIG_FILE" --enable CONFIG_MODULE_ALLOW_BTF_MISMATCH
          ./scripts/config --file "$CONFIG_FILE" --enable CONFIG_MODVERSIONS
          ./scripts/config --file "$CONFIG_FILE" --disable CONFIG_MODULE_FORCE_LOAD
          ./scripts/config --file "$CONFIG_FILE" --disable CONFIG_MODULE_SIG_FORCE

          # Deepongi'de açık
          ./scripts/config --file "$CONFIG_FILE" --enable CONFIG_TRIM_UNUSED_KSYMS

          # Deepongi'de kapalı
          ./scripts/config --file "$CONFIG_FILE" --disable CONFIG_SYSVIPC || true

          ./scripts/config --file "$CONFIG_FILE" --set-str CONFIG_LOCALVERSION "-deepongi"
          ./scripts/config --file "$CONFIG_FILE" --disable CONFIG_LOCALVERSION_AUTO

          sed -i 's/check_defconfig//' build.config.gki || true

      - name: Build kernel
        run: |
          cd common
          export PATH="$GITHUB_WORKSPACE/google-clang/bin:$PATH"

          sed -i 's/^PHONY += all/  /g' Makefile || true
          touch .scmversion
          ulimit -n 65535 || true

          printf '%s\n' '#!/bin/sh' 'exit 0' > scripts/check-local-export
          chmod +x scripts/check-local-export

          make O=out ARCH=arm64 LLVM=1 LLVM_IAS=1 \
            CC=clang LD=ld.lld HOSTCC=clang HOSTCXX=clang++ HOSTLD=ld.lld \
            AR=llvm-ar NM=llvm-nm OBJCOPY=llvm-objcopy OBJDUMP=llvm-objdump STRIP=llvm-strip \
            gki_defconfig

          make O=out ARCH=arm64 LLVM=1 LLVM_IAS=1 \
            CC=clang LD=ld.lld HOSTCC=clang HOSTCXX=clang++ HOSTLD=ld.lld \
            AR=llvm-ar NM=llvm-nm OBJCOPY=llvm-objcopy OBJDUMP=llvm-objdump STRIP=llvm-strip \
            olddefconfig

          echo "=== CONFIG CHECK ==="
          grep -E "CONFIG_MODULE_ALLOW_BTF_MISMATCH|CONFIG_LTO|CONFIG_DEBUG_INFO_BTF|CONFIG_MODVERSIONS|CONFIG_TRIM_UNUSED_KSYMS|CONFIG_SYSVIPC|CONFIG_CMDLINE" out/.config || true

          THREAD_FLAG=""
          if ld.lld --help | grep -q -- '--no-threads'; then
            THREAD_FLAG="--no-threads"
          elif ld.lld --help | grep -q -- '--threads='; then
            THREAD_FLAG="--threads=1"
          fi
          echo "LLD thread flag: ${THREAD_FLAG}"

          make -j1 O=out ARCH=arm64 LLVM=1 LLVM_IAS=1 \
            CROSS_COMPILE=aarch64-linux-gnu- \
            CC=clang LD=ld.lld HOSTCC=clang HOSTCXX=clang++ HOSTLD=ld.lld \
            AR=llvm-ar NM=llvm-nm OBJCOPY=llvm-objcopy OBJDUMP=llvm-objdump STRIP=llvm-strip \
            LDFLAGS_vmlinux="--export-dynamic ${THREAD_FLAG}" \
            Image || { echo "BUILD FAILED"; exit 1; }

          test -f out/arch/arm64/boot/Image || { echo "HATA: Image oluşmadı"; exit 1; }

          echo "BOOTHASH=VendorVisible-$(date +%s%N | cut -c1-10)" >> "$GITHUB_ENV"

      - name: Pack AnyKernel3
        run: |
          cd AnyKernel3
          sed -i 's/kernel.string=.*/kernel.string=MI14 Vendor Visibility Test/' anykernel.sh
          sed -i 's/do.devicecheck=1/do.devicecheck=0/' anykernel.sh
          sed -i 's/is_slot_device=0/is_slot_device=auto/' anykernel.sh
          sed -i '/is_slot_device=auto/a device.name1=houji\ndevice.name2=shennong\ndevice.name3=aurora\ndevice.name4=xiaomi14' anykernel.sh
          cp ../common/out/arch/arm64/boot/Image .
          zip -r ../MI14-VendorVisible-Test.zip ./*

      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.BOOTHASH }}
          name: "MI14 Vendor Visibility Test"
          files: "MI14-VendorVisible-Test.zip"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
